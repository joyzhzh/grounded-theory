# Data boundary

No real study data belongs in this reusable tool repository. The `data/`
directory is deny-by-default in `.gitignore`; this README exists only to make
that boundary visible.

Use an external private study vault referenced through `.vault-root`. Examples
and tests must be wholly synthetic and must not resemble or encode a real
participant, creator, project, prompt, transcript, or source capture.
